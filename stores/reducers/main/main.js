import { createSlice } from '@reduxjs/toolkit'

export const Main = createSlice({
    name: 'Main',
    initialState: {
       
    },
    reducers: {
       
    }
})

// Action creators are generated for each case reducer function
export const { SetUser } = Main.actions

export default Main.reducer